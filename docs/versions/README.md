# Model docs

One doc per model, named for the model — see `CLAUDE.md`'s naming rule
(descriptive names, not version numbers).

Empty for now: no model has been built and evaluated under the current
frozen eval suite. The retired `v1` and `v1.1` docs were removed when the
board-word holdout went from 60 to 150 words, which made their numbers
non-comparable with anything trained since. Their measurements are still
in [`../log.md`](../log.md).
